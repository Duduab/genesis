/**
 * OpenAPI-generated types for the Genesis HTTP API.
 *
 * Regenerate when the contract changes:
 *
 * ```bash
 * npm run gen:api-types
 * ```
 *
 * Source URL (see `package.json` → `scripts.gen:api-types`):
 * `https://genesis-api-242231160010.me-west1.run.app/openapi.json`
 *
 * If `npm run gen:api-types` fails (TLS/proxy), download the JSON in a browser or with `curl`,
 * save it as `docs/openapi.json`, then:
 *
 * ```bash
 * npx openapi-typescript docs/openapi.json -o src/types/genesis-api.ts
 * ```
 *
 * This placeholder exists so the repo builds before the first successful codegen run.
 */
export interface paths {}

export interface webhooks {}

export interface components {}

export interface operations {}

export type external = Record<string, never>

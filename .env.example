# Copy to `.env` (`.env` is gitignored — never commit secrets).
# Genesis API — optional override (see src/config/genesisEnv.ts).
# VITE_GENESIS_API_BASE_URL=https://genesis-gateway-svc-242231160010.me-west1.run.app

# Optional dev bearer when no Firebase session (no "Bearer " prefix).
# VITE_GENESIS_API_BEARER_TOKEN=
# VITE_GENESIS_ADMIN_BEARER_TOKEN=

# Firebase Web SDK (public client config). After sign-in, the app sends:
#   Authorization: Bearer <ID token from auth.currentUser.getIdToken()>
# Authorized domains (backend): localhost:3000, localhost:5173, genesis.il, hotpink-ram-291942.hostingersite.com
VITE_FIREBASE_API_KEY=AIzaSyADvUMYHA0hMIqhH_MtjzSrryKomkACt_A
VITE_FIREBASE_AUTH_DOMAIN=genesis-platform-il.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=genesis-platform-il
VITE_FIREBASE_STORAGE_BUCKET=genesis-platform-il.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=242231160010
VITE_FIREBASE_APP_ID=1:242231160010:web:945548713db0f0dd35d833
# Optional, Analytics only:
# VITE_FIREBASE_MEASUREMENT_ID=

# Pilot Email/Password user (enabled on backend): test@genesis.il — password from your ops / handoff (do not commit passwords to git).

# Optional: show legacy demo UI (Mission Control defaults to API-driven).
# VITE_GENESIS_SHOW_DEMO_UI=true
